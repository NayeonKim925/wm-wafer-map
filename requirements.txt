# ---------------------------------------------------------------------------
# Runtime dependencies for the WM-811K wafer-map defect classifier.
#
#   git clone ... && pip install -r requirements.txt && python train.py
#
# The dataset itself is downloaded automatically at runtime via kagglehub;
# no data files are shipped in this repository.
# ---------------------------------------------------------------------------

# Deep learning
torch>=2.0.0

# Numerics / data handling
numpy>=1.24.0
pandas>=2.0.0

# Dataset acquisition (Kaggle -> local cache)
kagglehub>=0.3.0

# Metrics, stratified splitting, label encoding
scikit-learn>=1.3.0

# Image resizing of variable-sized wafer maps
Pillow>=10.0.0

# Configuration files
PyYAML>=6.0

# Progress bars for long-running loops
tqdm>=4.65.0

# Confusion-matrix / training-curve visualisations (optional at runtime;
# imported lazily and skipped gracefully if unavailable)
matplotlib>=3.7.0
